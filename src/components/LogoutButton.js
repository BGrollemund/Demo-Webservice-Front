import React from "react";

const LogoutButton = () => {
    return (
        <div className="recipe-box logout">
            <div className="logout">
                <div>Logout</div>
            </div>
        </div>
    );
};

export default LogoutButton;